# Logical-Functions-Lookup-Assignment-003
Logical functions (IF, AND, OR) test conditions and return results, while lookup functions (VLOOKUP, HLOOKUP, INDEX, MATCH) search tables to fetch related data. Together, they simplify decisions and automate analysis in Excel.
